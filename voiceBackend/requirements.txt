# ============================================================
# Voice AI Agent — Python Backend Requirements
# Pinned for reproducibility. Update carefully.
# ============================================================

# LiveKit Agent Framework (with all plugins we need)
livekit-agents[deepgram,cartesia,silero,turn-detector,groq]~=1.5
livekit-api~=1.1

# HTTP client for server tools
httpx>=0.27.2
python-dotenv>=1.0

# Data validation
pydantic>=2.9.2
pydantic-settings>=2.5.2

# Utilities
structlog>=24.4.0
tenacity>=9.0.0
